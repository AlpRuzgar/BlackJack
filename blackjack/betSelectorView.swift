//
//  betSelectorView.swift
//  blackjack
//
//  Created by Alp Rüzgar on 25.02.2026.
//

import Foundation
import SwiftUI
/*
struct betSelectorView: View {
    @ObservedObject var viewModel = ContentViewModel()

    var body: some View {
        if viewModel.isGameOver {
            VStack {
                HStack {
                    ButtonView(action: {
                        
                    }, text: "-    ", backgroundColor: .red, textColor: .white)
                    .font(.system(size: 20, weight: .bold, design: .rounded))
                    
                    Text("Bet: \(viewModel.betAmount)")
                        .font(.largeTitle)
                        .padding()
                    
                    ButtonView(action: {
                        
                    }, text: "+", backgroundColor: .green, textColor: .white)
                }
                ButtonView(action: {
                    viewModel.isGameOver = false
                    Task {
                        await viewModel.startGame()
                    }
                }, text: "Place the bet                  ", backgroundColor: .blue, textColor: .white)
            }
        }
    }
}

#Preview {
    betSelectorView()
}
*/
